[project]
name = "encyhub"
version = "1.0.0"
description = "EncyHub - 开发工具聚合平台"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "psutil>=6.0",
    "websockets>=12.0",
]

[tool.uv]
dev-dependencies = [
    "pyinstaller>=6.0",
]
